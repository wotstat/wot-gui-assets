(def element ClanRewardWindowContent() layout=true entrance=true
    (macro RewardWindowContentModel)
    (scope
        (var descTxtStr:str = "R.strings.ingame_gui.rewardWindow[eventName].descText()")
    )
    (macro RewardWindowBase descText="descTxtStr")

    (style
        (minHeight = 525)
    )
)