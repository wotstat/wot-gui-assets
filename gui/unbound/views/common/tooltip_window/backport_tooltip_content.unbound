(def element BackportTooltipContent() layout = true entrance=true
    # This element is empty, it should be created to simulate usual view/window lifecycle.
    # On Python side - presenter for this view calls old Tooltip manager to show old tooltips.
    # Normally this mechanic should not be used. All new tooltips should be written on Unbound.

    (visible = false)
)
